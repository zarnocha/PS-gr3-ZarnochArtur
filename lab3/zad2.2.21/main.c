#include <stdio.h>
#include <stdlib.h>

unsigned long long ciag(unsigned long long n)
{
    unsigned long long a_jeden = (2*1)+5;
    for(int i=1; i<n; i++)
    {
        a_zero = 1;

    }
    unsigned long long a_n = (2*a_nminus)+5;
}

int main()
{
    unsigned long long n;
    printf("Podaj nieujemna calkowita liczbe: ");
    scanf("%d",&n);
    if(n<0) return -1;

    return 0;

}
